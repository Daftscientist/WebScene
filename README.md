# WebScene

WebScene is a TypeScript-first, timeline-based Canvas video composition library for the web.

This repository contains the core package in `packages/webscene`, docs in `docs`, and runnable examples in `examples`.
