# WebScene Examples

Run all examples with:

```bash
npm run dev:examples
```

Available demos:

- `basic-shapes`: animated rect + text + effects
- `text-kinetics`: text motion and color/opacity tracks
- `precomps-nesting`: nested composition workflow
- `export-frames`: PNG frame export + manifest
- `advanced-demo`: combined player, effects, and timeline tracks
